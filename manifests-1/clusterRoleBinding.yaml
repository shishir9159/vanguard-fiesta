apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: vanguard
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: vanguard
subjects:
  - kind: ServiceAccount
    name: vanguard
    namespace: default
